import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// Renderiza el componente principal App dentro del div con id="root"
ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
